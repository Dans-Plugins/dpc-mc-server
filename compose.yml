services:
  dpc-mc-server:
    build: .
    image: dpc-mc-server
    container_name: dpc-mc-server
    ports:
      - "25565:25565"
    restart: unless-stopped
    volumes:
      - dpcmcserver:/dpcmcserver

volumes:
  dpcmcserver: